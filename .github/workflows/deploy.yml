name: Deploy
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install Rust
        run: |
          rustup target add wasm32-unknown-unknown
          rustup toolchain install stable
          rustup default stable
      - name: Install latest mdbook
        run: |
          tag=$(curl 'https://api.github.com/repos/rust-lang/mdbook/releases/latest' | jq -r '.tag_name')
          url="https://github.com/rust-lang/mdbook/releases/download/${tag}/mdbook-${tag}-x86_64-unknown-linux-gnu.tar.gz"
          mkdir mdbook
          curl -sSL $url | tar -xz --directory=./mdbook
          echo `pwd`/mdbook >> $GITHUB_PATH
      - name: Install wasm-bindgen
        run: cargo install --force wasm-bindgen-cli
      - name: Build Book
        run: |
          mdbook build quickstart-book
      - name: Build wasm game
        run: |
          cargo build --target wasm32-unknown-unknown --release
          wasm-bindgen --out-dir quickstart-book/book --out-name quickstart-book --target web target/wasm32-unknown-unknown/release/bevy-quickstart-book.wasm
      - name: Optimize Wasm
        uses: NiklasEi/wasm-opt-action@v2
        with:
          file: quickstart-book/book/bevy-quickstart-book_bg.wasm
      - name: Copy assets
        run: cp -r assets quickstart-book/book/
      - name: Copy restart-audio-context.js for wasm
        run: cp wasm/restart-audio-context.js quickstart-book/book/
      - name: Setup Pages
        uses: actions/configure-pages@v4
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "quickstart-book/book"
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
